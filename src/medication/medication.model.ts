// medication.model.ts
export interface Medication {
    id?: string; // Optional, used for identification
    name: string;
    dosage: string;
    frequency: string;
    // Add more fields as needed
  }
  